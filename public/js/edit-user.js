//Function to edit post
async function editFormHandler(event) {
    event.preventDefault();

    //Get username, user id, email and password from the form
    let username = document.querySelector('input[name="username"]').value.trim();

    
}